# 大语言模型原理

大语言模型是一种**概率统计**模型，根据给定`词`的上下文，预测最可能的下一个`词`。

## 训练

大模型训练的本质就是“填空题”，即计算给定词和下一个可能词的权重（注意力分数）。

**注意力机制**

自注意力机制的底层逻辑——用数学计算**全局关联**。

**反向传播**

大语言模型也是一种神经网络。训练时候，当模型预测错误的时候，反向传播机制启动，从模型的输出端开始，一层一层倒着往回推，寻找到导致该结果的注意力分数，然后进行调整，也即增加或减小权重。

**梯度下降**

## 生成
